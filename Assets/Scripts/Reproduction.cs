using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reproduction : MonoBehaviour
{

    public float animalHealth;
    public float maxNumberOfOffspringsBeforeDeath; //Convert it ot int once in use
    public float maxNumberOfBabiesItCanHave; //Convert it ot int once in use(Random between 0 and max)

    









    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
